{
  "name": "pwa-counter",
  "version": "0.1.0",
  "description": "Node setup to package PWA for nginx",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "build-old": "npm run build-sw-old && mkdir -p html/landing_page && rsync -av --include='*/' --include='*.html' --include='*.png' --include='manifest.json' --include='favicon.ico' --include='*.css' --include='*.svg' --exclude='*' app/ html/",
    "build-sw-old": "rm -rf html/*.js && npx tsc"
  },
  "private": true,
  "dependencies": {
    "lucide-react": "^0.378.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.14.0",
    "vite-plugin-pwa": "^1.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@types/serviceworker": "^0.0.194",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.9.3",
    "vite": "^5.0.0"
  }
}
